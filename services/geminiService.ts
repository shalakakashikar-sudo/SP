
// AI Service has been disabled for the offline version of the app.
// This file is kept to prevent import errors if referenced elsewhere, 
// but it contains no logic.

export const generateVocabulary = async () => { return []; };
export const generateIdioms = async () => { return []; };
export const generateStoryPrompt = async () => { return ""; };
